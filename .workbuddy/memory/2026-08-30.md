# 2026-08-30

## 战纹 RTS 项目推送 GitHub
- 仓库：`honestfoxXXX/zhanwen-rts`（公开 Public）
- 链接：https://github.com/honestfoxXXX/zhanwen-rts
- 操作：用 `gh` 创建仓库并 `git push -u origin main`（170 个对象）；已加 topics（rts/typescript/vite/html5-game/game）。
- 环境注意：`gh` 安装在 `/opt/homebrew/bin/gh`（不在默认 PATH），需写全路径；账号 honestfoxXXX 已登录（repo/workflow 权限）。
- 本地 git 在 `main` 分支，`.gitignore` 已排除 node_modules/dist/.zcode 等。

## 启用 GitHub Pages
- 站点：https://honestfoxxxx.github.io/zhanwen-rts/ （用户名在 Pages 域名规范为小写 x）
- vite.config.ts 的 `base` 为 `'./'`（相对路径），天然兼容 Pages 子路径，无需改配置、不影响本地 dev。
- 部署方式：`npm run build` → 将 `dist/` 作为独立 git 仓库 force-push 到 `gh-pages` 分支（用 `gh auth git-credential` 凭证助手，不暴露 token）；Pages 源设为 `gh-pages` 分支根目录。
- 注意：`dist/` 内会被 git init 出独立仓库（dist 在主仓库 .gitignore 中，不干扰 main）。后续重新部署需重跑 build + push dist 到 gh-pages（可改为 GitHub Actions 自动部署，待定）。
